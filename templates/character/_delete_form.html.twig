<form method="post" action="{{ path('app_character_delete', {'id': character.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce personnage');" class="mt-5 mb-5">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ character.id) }}">
	<button class="btn btn-danger">Supprimer le personnage</button>
</form>
